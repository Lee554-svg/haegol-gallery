<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>💀 해골방 갤러리</title>
</head>
<body>
  <h1>💀 해골방 갤러리</h1>

  <form action="/" method="GET">
    <input type="text" name="q" value="<%= searchQuery || '' %>" placeholder="검색어 입력" />
    <button type="submit"><%= searchQuery ? searchQuery + ' 검색' : '검색' %></button>
  </form>

  <a href="/golnym">🔥 골념글 보기 (👍10개 이상)</a> | <a href="/write">[글쓰기]</a>

  <table border="1" cellpadding="5" cellspacing="0">
    <thead>
      <tr>
        <th>번호</th>
        <th>제목</th>
        <th>작성자</th>
        <th>작성시간</th>
        <th>👍 갈추 / 👎 문추</th>
      </tr>
    </thead>
    <tbody>
      <% posts.forEach((post, idx) => { %>
        <tr>
          <td><%= posts.length - idx + (currentPage - 1) * POSTS_PER_PAGE %></td>
          <td>
            <% if (post.imageUrl) { %>
              <img src="<%= post.imageUrl %>" alt="첨부 이미지" style="height: 30px;" />
            <% } %>
            <a href="/post/<%= post.id %>"><%- post.safeTitle %></a>
          </td>
          <td><%= post.author %></td>
          <td><%= post.createdAt %></td>
          <td>👍 <%= post.upvotes %> / 👎 <%= post.downvotes %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <% if (totalPages > 1) { %>
    <div>
      <% for(let i = 1; i <= totalPages; i++) { %>
        <% if(i === currentPage) { %>
          [<%= i %>]
        <% } else { %>
          <a href="/?page=<%= i %>">[<%= i %>]</a>
        <% } %>
      <% } %>
    </div>
  <% } %>

</body>
</html>




